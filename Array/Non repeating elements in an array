arr=[0, -2, 5, 7, -2, 0, 7]
seen=[]
repeated=[]
unique=[]
for i in range(len(arr)):
    if arr[i] in seen:
        repeated.append(arr[i])
        
    else:
        seen.append(arr[i])
        
        
for num in seen:
    if num not in repeated:
        unique.append(num)
print(unique)
